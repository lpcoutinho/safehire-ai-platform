# Provider para AWS real (staging/production)
# Para usar: remova providers-local.tf e mantenha este arquivo
provider "aws" {
  region = var.aws_region

  default_tags {
    tags = local.default_tags
  }
}
